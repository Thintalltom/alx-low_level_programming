#include<stdio.h>
/**
 * main - return the value 0 and also printnumber 0 to 10
 *
 * Return: Always (Success)
 */

int main(void)
{
int a;
for (a = 0; a < 10; a++)
printf("%d", a);
printf('\n');
return (0);
}
