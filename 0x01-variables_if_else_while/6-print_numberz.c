#include <stdio.h>
/**
 * main - print the lowercase alphabet
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int ch;

for (ch = 0; ch <= 9; ch--)
{
putchar(ch);
}
putchar('\n');
return (0);
}
