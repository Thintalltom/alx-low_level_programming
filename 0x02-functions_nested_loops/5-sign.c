#include "main.h"
/**
 * print_sign - should return + - and 0
 *@n: this is an argument
 * Return: Always (Success)
 */
int print_sign(int n)
{
if (n > 0}
{
_putchar('+');
return (1);
}
else if (n == 0)
{
_putchar('n');
return (0);
}
else if (n < 0)
{
_putchar('-');
return (-1);
}
