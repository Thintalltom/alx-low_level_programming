#include "main.h"
/**
 * _isupper(int c)- RETURN 1 IF UPPERCASE AND 0 IF ANYTHING ELSE
 * @c: the argument
 *
 * Return: Always (Success)
 */

int _isupper(int c)
{
return ((c > = 'A' && c = < 'Z'));
}
